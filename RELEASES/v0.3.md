# Release v0.3

Release: v0.3 (stable)

What's included:
- include/E-Dialog.inc (stable v0.3)
- example/example.pwn demonstrating core and new helper functions
- README.md with per-function installation and usage

Notes:
- This release focuses on usability: per-player queueing, auto-padding helpers to avoid using '\t', input sanitization, and a small in-memory logger.
- To create an official GitHub release tag, create a tag named `v0.3` in the repo or use the GitHub Releases UI.
